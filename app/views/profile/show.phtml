<div class="d-flex align-items-start gap-4 p-4 bg-light rounded shadow-sm">
    <div class="text-center">
        <img 
            id="image_preview" 
            class="avatar rounded-circle img-thumbnail mb-3" 
            src="<?= $this->current_user->avatar()->path() ?>" 
            alt="Foto de perfil" 
            style="width: 150px; height: 150px; object-fit: cover;">

        <form action="<?= route('profile.avatar') ?>" method="POST" enctype="multipart/form-data">
            <div class="mb-2">
                <input 
                    id="image_preview_input" 
                    type="file" 
                    name="user_avatar" 
                    accept="image/*" 
                    class="form-control form-control-sm">
            </div>
            <div class="d-grid">
                <button 
                    id="image_preview_submit" 
                    type="submit" 
                    class="btn btn-primary btn-sm">
                    Atualizar foto
                </button>
            </div>
        </form>
    </div>

    <div class="flex-grow-1">
        <div class="mb-2">
            <h3 class="mb-0"><?= $this->current_user->name ?></h3>
        </div>

        <ul class="list-unstyled mt-3">
            <li><strong>Email:</strong> <?= $this->current_user->email ?></li>
        </ul>
    </div>
</div>
